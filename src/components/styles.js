import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    width: 300,
    height: 100,
    marginTop: 20,
    borderColor: "black",
    borderWidth: 2,
    borderRadius: 4,
    justifyContent: "center",
    alignItems: "center"
  },
  text: {
    fontSize: 20
  }
});
